# Decision Log — SPEC-companion-connection-chat-ux

## 2026-09-08 创建（bmad-spec，express 蒸馏）

- **输入**：`companion-connection-chat-ux-investigation.md`（调查，证据分级定案）+ 用户产品裁决与工作流指令（完整、无需 elicitation）。
- **操作**：create。slug `companion-connection-chat-ux` 继承调查档案命名；无同名既有 spec（唯一既有 spec 为 `spec-mobile-fr-parity`，无 slug 冲突）。
- **结构**：内核 7 项能力（CAP-1~7：非阻断体验 / 统一状态模型 / 凭据恢复闭环 / QR 生命周期 / 命令就绪 / 流式契约 / IME）；7 个 spec-authored companions（state-and-recovery-model、qr-and-unbind-semantics、streaming-protocol、ime-diagnosis、task-breakdown、test-matrix、supersessions）+ 1 个 adopted（../../project-context.md）。调查档案归入 `sources:`（load-bearing 断言已全部吸收并保留证据等级标注）。
- **关键取舍**：
  1. 状态模型采用"三正交面"（TransportStatus / PairingHealth / commandReady）而非单一大枚举——避免把展示与可操作判据再次耦合（这正是本次要修的分裂事实源病灶）。
  2. `identityRevoked` 由"手机本地已配对 + 公钥未变 + 桌面结构化拒绝"推断，不在桌面侧新增撤销名单存储（避免范围蔓延）。
  3. 双端一键解绑列为 Non-goal（用户指令为条件式"如果设计"，本规格仅做语义区分与文案）。
  4. Reconnecting 发送策略选"立即禁用 + 提示"，不引入聊天发送队列（chat.send 非幂等，队列化引入重复执行风险）。
  5. 结构化拒绝复用 `Frame.Notice`（无帧协议变更，双端独立可发布）。
  6. IME 严格两阶段，A 阶段先行独立交付（证据门控，AGENTS 规则十三）。
- **自校验 Pass 1（Coherence）**：7 能力均含 intent+success；intent 描述 WHAT（方法细节下沉 companion）；9 条 Constraints 均可裁定设计（范式唯一 / 事实源唯一 / 掩盖禁令 / 证据边界 / 协议兼容 / 外科手术等）；Non-goals 7 项显式；成功信号绑定 11 条可执行验收 + 构建命令；CAP ID 唯一稳定；行文按 Spec Law 8 收敛。无伪造输入未支持的承诺。
- **自校验 Pass 2（Preservation）**：逐条走查用户裁决（6 条产品裁决）、6 个工作流（含全部子条款：20s grace 起止、bind/unbind 时序、loadExisting 拆分、原子协调六步、QR 四项、五值原因码、共用判定函数、契约测试矩阵、IME 两阶段与 A/B 变量）、11 条测试验收、输出要求（P0/P1+顺序、FR-43 取代表、证据边界、统一模型贯穿、暂停待批）——全部落位。调查 Findings 1-12、Deductions 1-6、Hypotheses 1-4（开放项按证据等级标注：H1→IME A 阶段、H2→测试 #8、H3→分类表、H4→deferred）、Recommended Next Steps A-E、Side Findings（含备份/恢复触发源注记）均吸收。Wrapper-only 丢弃：调查的 Hand-off 叙事、Timeline 时间线、Evidence Inventory 表格（过程记录，断言已按 file:line 锚点吸收进契约）、Missing Evidence 表（已转化为测试缝与诊断要求）。
- **结论**：PASS。规格待人工批准后进入实施（批准前不改动任何产品代码；T-S9 治理标注动作依赖本批准）。

## 2026-09-08 人工裁决落地（两项 Open Questions 拍板）

- **裁决 ①（离线输入形态）**：不设速记条；无遮罩，直接在管家对话界面输入；网络不可用时仅提示"网络不可用，网络正常了会继续发送"；速记条弃用。
  - 落地：新增 **T-S10 对话离线待发箱**（`sync/ChatOutbox.kt`：FIFO、幂等 commandId、进程级；`!commandReady` 入队、`commandReady && paired` 串行 flush、一次一条等流式 done）；`QuickNoteQueue` 与速记条 UI 随遮罩退役删除（证据：`QuickNoteQueue.flush()` 为纯内存 mock、从未真实发送——新设计顺带闭合该缺口）；桌面 dispatcher 幂等缓存全指令通用（`companion_dispatch.rs:102-133`），重发复用 commandId 零重复执行。
  - **对原推荐的取代**：创建条目"关键取舍 4（不引入发送队列）"被本裁决推翻——chat.send 重复执行风险由 commandId 复用消解。
  - **对验收 #8 字面的修订（显式呈请）**："竞速窗口发送控件必须不可用"修订为"发送不产生突兀失败、落入待发箱、bind 后自动发出"；意图（不允许界面显示正常、点击后突兀报"桌面引擎不可达"）不变。非对话写操作（Tasks/Memory/ActionCard 等）仍按原产品裁决逐项禁用；Onboarding 引导流不接入待发箱。
- **裁决 ②（fixture 维护）**：A（手工快照）与 B（桌面 phase 值域锁定测试）都做——B 从"P1 可选推荐"升级为必做，并入 T-S1。
- **同步更新**：SPEC.md（CAP-1/CAP-3/CAP-5、Constraints、Assumptions，Open Questions 清空）、state-and-recovery-model.md（§1 消费面、§4.4-4.6、§5.2）、task-breakdown.md（T-S1/T-S2/T-S3 落点、T-S10 新增、顺序图、验收摘要）、test-matrix.md（#5/#8/#11、机制级两行、回归清单）、supersessions.md（FR-43 速记条款形态取代、冻结规格速记条/flush 守门条、PRD 修订动作）、ime-diagnosis.md（§B.4 输入面收敛）、streaming-protocol.md（§4 桌面锚定必做）。
- **自校验（复检）**：Pass 1——CAP-1/CAP-5 与新裁决语义一致、约束可裁定、Open Questions 清零无悬空引用；Pass 2——两项裁决全部子语义（提示文案、幂等、串行、结果处理、三场景不丢失、非对话操作禁用边界、Onboarding 边界、A+B 双轨）均已落位；全仓 grep 无残留旧语义引用（quickNotes 仅存于"退役/删除"语境）。结论：PASS，仍暂停等待人工批准。

## 2026-09-08 拍板收口（三项分叉确认，规格定稿）

- **裁决 ①A 确认**：竞速窗口/网络空档发送 → 入待发箱，不禁用、不突兀报错；验收 #8 修订正式定案（取代上一条目中"按队列裁决修订"的待确认表述）。
- **裁决 ②B**：待发箱落盘持久化——队列写入应用私有目录（复用既有 `KeystoreSnapshotCipher` 加密、原子写，镜像 `PairingSecrets.atomicWrite` 模式），进程被杀/冷启动后恢复队列与待发态气泡（含本地占位会话）；T-S10 落点与验收扩容；#11 验收增加"进程被杀"场景（U：进程重建恢复断言；M：断网录入→强杀重开→恢复网络自动送达）。
- **裁决 ③默认（未回复视为采纳）**：Onboarding 引导流不接入待发箱。
- 同步更新：SPEC.md（约束"不可丢失"扩围、假设两条改定稿）、state-and-recovery-model.md（§4.4 落盘条目）、task-breakdown.md（T-S10 落点/验收）、test-matrix.md（#11）、supersessions.md（FR-43 速记条款）。
- **自校验（终检）**：Coherence——"进程级"表述已全部替换为落盘语义，约束/任务/测试三处一致；Preservation——三项裁决落位完整。PASS。**规格无剩余待拍板项，等待最终批准后按 T-S1 开工。**

## 2026-09-10 后续裁决（spec-companion-android-remove-onboarding）

- **用户裁决：手机端引导流程整体移除**（FR-21 移动侧退役，桌面引导保留，初始设置统一在桌面完成）。上文「裁决 ③默认：Onboarding 引导流不接入待发箱」及裁决 ① 记录尾部的「Onboarding 引导流不接入待发箱」子条款随之失去对象；待发箱边界此后仅约束 Chat 流（与实现现状一致——OnboardingViewModel 从未接入 commandSender/chatOutbox，引导包已整包删除）。
- 同步更新：SPEC.md 假设（Onboarding sendMessage 行）、task-breakdown.md（T-S2 的 Onboarding 路由成员）、state-and-recovery-model.md（§4.4-6 Onboarding 边界）已行内批注失效；supersessions.md 追加 §6 登记。
