[package]
name = "companion-proto"
version = "0.1.0"
edition = "2021"
description = "EgoSync 手机伴侣三端共享加密协议 crate：8 种帧 schema 冻结 + Noise XX 加密互通"

[lib]
name = "companion_proto"
path = "src/lib.rs"

[dependencies]
snow = "0.10"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
