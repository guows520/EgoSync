# sqlx migrations: checksum 必须与开发环境一致，禁止 git 在 Windows runner 上做 LF→CRLF 转换
# 否则 sqlx migrate! 宏编译时嵌入的 checksum 与旧数据库记录不匹配，导致升级安装后闪退
egosync-app/src-tauri/migrations/*.sql text eol=lf
# index.html 内联防 FOUC 脚本：CSP sha256 为 byte 对 byte 契约
# （csp.contract.test.ts）——Windows 检出时 autocrlf 的 LF→CRLF 会改变
# 脚本字节，测试哈希失配（CI Windows 腿红）且 Windows 本机构建的 dist
# 会真实触发 CSP 拒执行主题脚本。钉 LF 与上方 sqlx 迁移同款问题同款解法。
egosync-app/index.html text eol=lf
# src-tauri/lib.rs：16.3 落地三个「源码扫描钉」测试（remote_mode 守卫
# 序/重启清理序/远程注册面）内含跨行字面量匹配（"\n                return
# Ok(());"）——Windows autocrlf 的 LF→CRLF 使匹配失配（CI Windows 腿红）。
# 钉 LF 与 sqlx 迁移/index.html 同款问题同款既定解法。
egosync-app/src-tauri/src/lib.rs text eol=lf
