# Epic 7 回顾：数据主权 (Data Sovereignty)

**日期**：2026-06-27
**参与者**：boss
**Epic 状态**：4 个故事全部完成，零延迟

---

## 完成情况

| Story | 状态 | 审查补丁 | 延迟项 |
|-------|------|---------|--------|
| 7-1 数据导出 | done | 2 Patch + 1 Decision + 2 Defer | 0 |
| 7-2 数据销毁 | done | 1 Decision→Patch + 2 Patch + 1 Defer | 0 |
| 7-3 数据 Tab 集成验证 | done | 1 Patch | 0 |
| 7-4 数据导入 | done | 5 Patch (全修复) | 0 |

---

## 做得好的方面

- **零延迟交付**：4 个故事全部完成，延续 Epic 6 的零延迟记录
- **代码审查发现关键 bug**：7-4 审查发现 SQLite 导入连接池 ATTACH 级别 bug（`max_connections(5)` 下 ATTACH 是连接级作用域，后续语句可能落到未挂载的连接），这是生产级严重问题，审查捕获并修复
- **架构约束识别到位**：7-4 识别出"Tauri State 不可替换 → 不能关闭/重连 DB 连接池"，采用 `ATTACH DATABASE` + 逐表 SQL 复制方案，避免了架构冲突
- **安全意识持续**：7-2 keyring 删除移到 DB 事务提交后（审查修复），避免"失败的销毁误删 API Key"；7-1 导出不包含 API Key 明文
- **7-3 验证型 story 模式有效**：7.1+7.2 已完成实现，7.3 专注集成验证+测试补齐，发现并修复 3 个预存测试 bug
- **备份机制一致复用**：7-2 销毁前备份 + 7-4 导入前备份，复用同一 `create_backup()` 函数，7 天自动清理

## 需要改善的方面

- **Dev Agent Record 占位未填**：7-1 和 7-4 的 `{{agent_model_name_version}}` 未更新——Epic 6 行动项"确保每个故事完成时更新 Dev Agent Record"未完全执行
- **7-4 SQLite 导入零测试覆盖**：审查前 SQLite 导入路径完全无测试，导致连接池 bug 未被发现。JSON 路径有测试但 SQLite 路径被遗漏
- **7-4 serde_json::Value 静默兜底**：`unwrap_or("")` / `.as_i64().unwrap_or(1)` 将缺失数据静默转为空串或魔法值，损失数据保真度
- **7-1 目录选择方案偏离 spec**：spec 写方案 A（前端 dialog），实际用方案 B（Rust rfd）。虽已接受，但 spec 与实现不一致的模式在 Epic 7 中重复出现（7-4 同样偏离）
- **跨库非原子操作**：7-2 主库+对话库分别事务，7-4 同样。部分失败会留下不一致状态，前端无专门提示（已 defer，架构限制）

## Epic 6 回顾行动项跟进

| 行动项 | 状态 | 说明 |
|--------|------|------|
| LLM 代码公共化（第3次提出） | ❌ 未执行 | Epic 7 不涉及 LLM 调用，未触发此问题，但根本问题未解决 |
| 调度器触发统一改造（V2候选） | ⏳ 未涉及 | Epic 7 无调度器，V2 候选 |
| Dev Agent Record 收尾流程 | ⏳ 部分执行 | 7-2/7-3 已填写，7-1/7-4 仍为占位符 |

## 行动项

1. **Dev Agent Record 收尾流程**（从 Epic 6 延续，第二次提出）：确保每个故事完成时更新 Status/File List/Dev Agent Record，7-1 和 7-4 仍有 `{{agent_model_name_version}}` 占位符未填
2. **spec 与实现偏离的文档化**：当实现方案偏离 spec 时，在 story 文件的 Review Findings 中明确记录偏离原因和接受决策（7-1 已做到，7-4 需补记目录选择偏离）
3. **SQLite 路径测试覆盖**：涉及 SQLite 文件操作的 story 必须覆盖 SQLite 路径的往返测试，不能只测 JSON 路径（7-4 审查教训）
4. **serde_json::Value 读取禁止静默兜底**：从 `Value` 取关键字段时，缺失/null 应返回 `ValidationError`，不允许 `unwrap_or` 静默转换（7-4 审查修复，需固化为编码规范）

## 下一个 Epic

**Epic 8：跨平台分发与 V1 加固 (Cross-Platform Distribution & V1 Hardening)**
- 8-1：GitHub Actions 三平台 CI
- 8-2：E2E 测试核心旅程
- 8-3：WCAG 无障碍审计
- 8-4：性能基准验证
- 8-5：Release 自动发布
- 依赖：所有 Epic 1-7 已完成，Epic 8 可启动
- E2E 测试需覆盖数据导出/销毁/导入流程（Epic 7 交付）
- **这是 V1 的最后一个 Epic**

---

*回顾方式：基于故事文件深度分析 + 代码审查发现 + Epic 6 行动项跟进，boss 选择跳过讨论直接收尾。*
