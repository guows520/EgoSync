# UAT 数据需求表 — {{date}}

> 以下数据**无法自动生成**，需由你提供真实物料。请将文件放入集中数据目录，并在"用户填写路径"列填入相对/绝对路径。
> 集中数据目录：`{data_dir}`

| 需求项 | 数据要求规格 | 归属用例 | 用户填写路径 |
|---|---|---|---|
| <如 真实证件照> | <如 清晰真实证件照，jpg/png，<2MB> | <UAT-KYC-001> | <在此填写文件路径> |
| | | | |

## 填写说明
1. 将所需文件复制到 `{data_dir}` 目录下。
2. 在对应行"用户填写路径"列填入路径（建议相对 `{data_dir}` 的路径）。
3. 填写完成后告知验收官，将运行数据完整性与可用性校验。
4. 校验不通过的项会列出原因，请按建议修正后重新校验。
